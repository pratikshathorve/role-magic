
import TeamRoleAllocator from "@/components/TeamRoleAllocator";

const Index = () => {
  return (
    <div className="min-h-screen bg-background">
      <TeamRoleAllocator />
    </div>
  );
};

export default Index;
